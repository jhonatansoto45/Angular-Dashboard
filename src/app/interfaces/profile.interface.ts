export interface AsideList {
  name: string;
  route: string;
}
export interface TeamMembersList {
  photo: string;
  name: string;
  dateAdded: string;
  lastActive: string;
}

export interface TeamsList {
  title: string;
  description: string;
  skills?: string[];
}
